apiVersion: 1

datasources:
  - name: influx
    type: influxdb
    uid: influx
    isDefault: true
    access: proxy
    url: http://influx:8086
    jsonData:
      version: Flux
      organization: my-org
      defaultBucket: health
      token: my-token
    secureJsonData:
      token: my-token
